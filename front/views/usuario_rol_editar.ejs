<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Cambiar Rol</title>
</head>
<body>

<h1>Cambiar Rol de Usuario</h1>

<p><strong><%= usuario.nombre %></strong></p>

<form method="POST">

    <label>Rol:</label>
    <select name="rol_id">
        <option value="1" <%= usuario.rol_id == 1 ? 'selected' : '' %>>
            Admin Sistema
        </option>
        <option value="2" <%= usuario.rol_id == 2 ? 'selected' : '' %>>
            Admin Club
        </option>
        <option value="3" <%= usuario.rol_id == 3 ? 'selected' : '' %>>
            Viewer
        </option>
    </select>

    <br><br>

    <label>Club (solo si es admin club)</label>

    <select name="club_id">
        <option value="">Sin club</option>

        <% if (clubes && clubes.length > 0) { %>
            <% clubes.forEach(c => { %>
                <option value="<%= c.id %>" 
                    <%= usuario.club_id == c.id ? 'selected' : '' %>>
                    <%= c.nombre %>
                </option>
            <% }) %>
        <% } %>
    </select>

    <br><br>

    <button type="submit">Guardar</button>

</form>

<hr>

<a href="/admin/usuarios">⬅ Volver</a>

</body>
</html>